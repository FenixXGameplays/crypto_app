name: crypto_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  fl_chart: ^0.69.2
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  go_router: ^14.6.2
  google_fonts: ^6.2.1
  http: ^1.2.2
  path_provider: ^2.1.5
  provider: ^6.1.2

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
